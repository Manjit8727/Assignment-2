<!--
    
Name: Manjit kaur
Student ID: 301134995
File name: Manjitkaur_COMP229_assignment2
Date: 23/10/2020

-->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
    <title>Edit the Contact</title>
    <style>
        *
        {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
        }
        .main
        {
            width: 100%;
            height: 100vh;
            background: #fff;
        }
        .edit-form
        {
            width: 30%;
            height: 30vh;
            margin-top: 12%;
            margin-left: 34%;
            position: absolute;
            background: #f6feff;
            border-radius: 10px;
            box-shadow: 3px 3px 0 #000,
                        -1px -1px 0 #000,  
                        1px -1px 0 #000,
                        -1px 1px 0 #000,
                        1px 1px 0 #000;
        }
        .input-text
        {
            margin-left: 20px;
            margin-top: 20px;
            padding: 10px;
            border: none;
            background: none;
            opacity: 1 !important;
            border-radius: 5px;
            border-bottom: 1px solid #000 !important;
        }
        .input-btn
        {
            margin-top: 30px;
            padding: 10px;
            margin-left: 40px;
            background: none;
            border: none;
            border-radius: 5px;
            border: 1px solid #000 !important;
        }
        .title
        {
            margin-left: 36%;
            font-size: 40px;
            margin-top: 5%;
            position: absolute;
        }
    </style>
</head>
<body>
    <section class="main">
        <h1 class="title">Edit the Contact List</h1>
        <form action="/editcontact" class="edit-form" method="POST">
            <input type="text" name="name" class="input-text" value="<%= contact.name %>">
            <input type="text" name="phone" class="input-text" value="<%= contact.phonenum %>">
            <input type="text" name="email" class="input-text" value="<%= contact.email %>">
            <input type="hidden" name="id" class="input-text" value="<%= contact.id %>">
            <button class="input-btn">Save Changes</button>
        </form>
    </section>
</body>
</html>
